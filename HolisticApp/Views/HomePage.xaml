<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HolisticApp.Views.HomePage"
             Title="Übersicht">
    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="UserToolbarItem" Text="??" Clicked="OnUserToolbarItemClicked" Priority="0" Order="Primary" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Padding="20" Spacing="20">
            <!-- Anamnese-Button -->
            <Button Text="Anamnese" Clicked="OnAnamnesisClicked" />

            <!-- Weitere Buttons -->
            <Button Text="Befunde hochladen" />
            <Button Text="Chat" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>